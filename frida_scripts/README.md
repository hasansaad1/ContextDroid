# Frida scripts

JavaScript hook definitions loaded by the extraction pipeline. `hook_apis.js` is the production hook set; its SHA256 is recorded in session metadata and in `export/v2_extended/PROVENANCE.md`.
