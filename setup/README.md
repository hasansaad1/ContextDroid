# Setup

Host bootstrap: Android SDK/emulator install (`install_dependencies.sh`), AVD creation (`setup_emulator.sh`), and optional standalone `adb` under `tools/platform-tools/` (`install_adb_platform_tools.sh`). Run once per machine before collection.
