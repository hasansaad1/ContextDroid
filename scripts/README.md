# Scripts

Operational helpers outside the main extraction loop. `scripts/safety/` holds phased malware-corpus gates (P0–P5), network sink tooling, and session pre/postflight checks — see `SAFETY.md`. `scripts/corpus/` holds sample selection, vault interaction, and `run_sample.py` (malware tier; do not run without go/no-go sign-off).
